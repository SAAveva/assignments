const config = {
	db: {
		host: 'localhost',
		user: 'assignment',
		password: '123456',
		database: 'assignment'
	},
	SESSION_MAX_TIME: 60 * 60 * 48, // in seconds, default: 48 hours
};

export default config;
